# Plan 16-02: Settings Panel Integration + Verification

## Objective
Add "Re-run Onboarding" button to settings panel, verify build, and ensure E2E test alignment.

## Tasks

### Task 1: Add re-run button to SettingsPanel
- Button text: "Run Onboarding Wizard" (matches E2E `/run onboarding|get started/i`)
- Calls `updateSettings({ onboardingComplete: false })` to trigger wizard

### Task 2: Build verification
- `npx tsc --noEmit`
- `npx biome check --write`
- `npm run build` (Vite)
- `cargo check` in src-tauri

### Task 3: E2E test alignment
- Verify E2E test selectors match actual UI text
- Ensure wizard hides after completion + persists across reload

## Files Changed
- `rulez_ui/src/components/settings/SettingsPanel.tsx`
