---
phase: 19-gemini-hooks-support
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - cch_cli/src/cli/gemini_install.rs
  - cch_cli/src/cli.rs
  - cch_cli/src/main.rs
  - cch_cli/tests/gemini_install.rs
autonomous: true

must_haves:
  truths:
    - "Users can install or remove RuleZ Gemini hooks for project, user, or system settings."
    - "Gemini extension hook configs can be created under ~/.gemini/extensions/<ext>/hooks/hooks.json."
    - "Gemini extension hook configs can be created under ~/.gemini/hooks/."
    - "Generated hook configs call the Gemini hook runner with correct event/matcher setup."
  artifacts:
    - path: "cch_cli/src/cli/gemini_install.rs"
      provides: "Gemini settings loader, hook config writer, extension support"
    - path: "cch_cli/tests/gemini_install.rs"
      provides: "Install/uninstall path and config tests"
    - path: "cch_cli/src/main.rs"
      provides: "CLI wiring for gemini install/uninstall commands"
  key_links:
    - from: "cch_cli/src/cli/gemini_install.rs"
      to: "~/.gemini/extensions/<ext>/hooks/hooks.json"
      via: "extension install path resolution"
      pattern: "extensions"
    - from: "cch_cli/src/cli/gemini_install.rs"
      to: "~/.gemini/hooks/"
      via: "extension install path resolution"
      pattern: "\.gemini/hooks"
    - from: "cch_cli/src/cli/gemini_install.rs"
      to: "settings.json"
      via: "merge hooks into settings"
      pattern: "hooks"
---

<objective>
Add Gemini CLI install/uninstall support, including extension hook placement, so RuleZ can be registered in Gemini settings consistently.

Purpose: Make Gemini hook integration usable without manual JSON editing.
Output: Gemini install/uninstall CLI, settings/extension config writers, tests.
</objective>

<execution_context>
@/Users/richardhightower/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-gemini-hooks-support/19-RESEARCH.md
@cch_cli/src/cli/install.rs
@cch_cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Gemini settings and extension hook installer</name>
  <files>cch_cli/src/cli/gemini_install.rs</files>
  <action>
Create a Gemini install module that:
- Defines settings structures for Gemini hooks and safely merges with existing settings.json content.
- Supports scopes (project/user/system) with explicit paths per Gemini docs and an extension mode that writes to ~/.gemini/extensions/<ext>/hooks/hooks.json.
- Supports extension hooks at both ~/.gemini/extensions/<ext>/hooks/hooks.json and ~/.gemini/hooks/ locations per Gemini extension guidance.
- Registers hook entries for Gemini lifecycle and tool events with correct matcher semantics (regex for tool hooks, exact strings for lifecycle hooks).
- Accepts a --binary override and defaults to the resolved cch binary path used by other install flows.
  </action>
  <verify>cargo test -p cch_cli gemini_install</verify>
  <done>Install/uninstall functions can create and remove Gemini hook entries for settings.json and extension hooks.json.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CLI commands and add installer tests</name>
  <files>cch_cli/src/cli.rs, cch_cli/src/main.rs, cch_cli/tests/gemini_install.rs</files>
  <action>
Expose new CLI commands (e.g., `cch gemini install` and `cch gemini uninstall`) with flags for scope and extension name, and add tests to validate path resolution and hook config rendering (including extension path behavior).
  </action>
  <verify>cargo test -p cch_cli gemini_install</verify>
  <done>CLI help shows Gemini install/uninstall commands and tests cover settings + extension hook file generation.</done>
</task>

</tasks>

<verification>
- `cargo test -p cch_cli gemini_install` passes
</verification>

<success_criteria>
- Gemini hooks can be installed/uninstalled for project/user/system scopes and extension hooks.
- Hook configs target the Gemini hook runner with correct matcher semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/19-gemini-hooks-support/19-02-SUMMARY.md`
</output>
