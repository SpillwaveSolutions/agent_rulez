---
phase: 11-rename-fix-settings-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rulez_ui/src-tauri/tauri.conf.json
  - rulez_ui/src-tauri/src/commands/config.rs
  - rulez_ui/src-tauri/src/commands/debug.rs
  - rulez_ui/src/components/simulator/DebugSimulator.tsx
  - rulez_ui/src/lib/tauri.ts
  - rulez_ui/src/lib/mock-data.ts
  - rulez_ui/src/lib/schema.ts
  - rulez_ui/src/types/index.ts
  - rulez_ui/public/schema/hooks-schema.json
  - cch_cli/src/logging.rs
autonomous: true
must_haves:
  truths:
    - "Window title and simulator copy refer to RuleZ, not CCH"
    - "New config templates and mock configs describe RuleZ configuration"
    - "Default audit log path resolves to ~/.claude/logs/rulez.log"
    - "Shell command scope name and command target RuleZ"
    - "No user-facing RuleZ UI/command/config descriptions mention CCH"
  artifacts:
    - path: "rulez_ui/src-tauri/tauri.conf.json"
      provides: "RuleZ window title and shell scope label"
    - path: "rulez_ui/src-tauri/src/commands/config.rs"
      provides: "Default config template text uses RuleZ"
    - path: "rulez_ui/src/components/simulator/DebugSimulator.tsx"
      provides: "User-facing simulator copy references RuleZ"
    - path: "rulez_ui/src/lib/mock-data.ts"
      provides: "Mock configs labeled as RuleZ"
    - path: "cch_cli/src/logging.rs"
      provides: "Default log path uses rulez.log"
  key_links:
    - from: "rulez_ui/src-tauri/tauri.conf.json"
      to: "app.windows[0].title"
      via: "window title"
      pattern: "RuleZ UI"
    - from: "rulez_ui/src-tauri/tauri.conf.json"
      to: "app.shell.scope"
      via: "scope name and command"
      pattern: "rulez"
    - from: "cch_cli/src/logging.rs"
      to: "Logger::default_log_path"
      via: "log file basename"
      pattern: "rulez\\.log"
---

<objective>
Replace CCH-facing strings with RuleZ in the UI and defaults, and update the CLI log path to rulez.log.

Purpose: Make the rename user-visible and align default artifacts with RuleZ naming.
Output: Updated UI labels, config templates, and default log path.
</objective>

<execution_context>
@/Users/richardhightower/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-rename-fix-settings-foundation/11-RESEARCH.md
@rulez_ui/src-tauri/tauri.conf.json
@rulez_ui/src-tauri/src/commands/config.rs
@rulez_ui/src-tauri/src/commands/debug.rs
@rulez_ui/src/components/simulator/DebugSimulator.tsx
@rulez_ui/src/lib/tauri.ts
@rulez_ui/src/lib/mock-data.ts
@rulez_ui/src/lib/schema.ts
@rulez_ui/src/types/index.ts
@rulez_ui/public/schema/hooks-schema.json
@cch_cli/src/logging.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update user-facing RuleZ labels and templates</name>
  <files>rulez_ui/src-tauri/tauri.conf.json, rulez_ui/src-tauri/src/commands/config.rs, rulez_ui/src/components/simulator/DebugSimulator.tsx, rulez_ui/src/lib/mock-data.ts</files>
  <action>Replace user-visible CCH references with RuleZ: update the Tauri window title to RuleZ wording, update the shell scope name and command target in tauri.conf.json from cch to rulez, adjust the default config template header in config.rs, update the debug simulator helper text, and rename mock config strings to RuleZ. Keep non-user-facing code structure unchanged.</action>
  <verify>Open the UI strings in these files and confirm no user-visible "CCH" remains, and the tauri.conf.json shell scope uses rulez for name/command.</verify>
  <done>Window title and simulator copy show RuleZ; shell scope name/command are rulez; new config templates and mock configs reference RuleZ.</done>
</task>

<task type="auto">
  <name>Task 2: Switch default audit log path to rulez.log</name>
  <files>cch_cli/src/logging.rs</files>
  <action>Update Logger::default_log_path and its doc comment to use ~/.claude/logs/rulez.log instead of cch.log while keeping directory structure unchanged.</action>
  <verify>rg "rulez\\.log" cch_cli/src/logging.rs</verify>
  <done>Default log path uses rulez.log and the comment matches.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep remaining UI/command/config strings for CCH</name>
  <files>rulez_ui/src-tauri/src/commands/debug.rs, rulez_ui/src/lib/tauri.ts, rulez_ui/src/lib/schema.ts, rulez_ui/src/types/index.ts, rulez_ui/public/schema/hooks-schema.json</files>
  <action>Run a global search for user-facing "CCH"/"cch" references in RuleZ UI, command descriptions, and config-path text, then update remaining labels and messages to RuleZ. Preserve internal binary names or file paths that must stay cch for execution; only user-visible copy should change.</action>
  <verify>rg -n "CCH|cch" rulez_ui/src rulez_ui/src-tauri/src/commands rulez_ui/public/schema</verify>
  <done>Remaining user-facing UI/command/config labels and descriptions use RuleZ and no unintended CCH labels remain in these areas.</done>
</task>

</tasks>

<verification>
- Manual scan confirms no user-visible CCH labels remain in the touched UI strings.
- Global search confirms remaining CCH hits in RuleZ UI/command/config files are intentional (non-user-facing) or removed.
- Default log path is rulez.log in the CLI logger.
</verification>

<success_criteria>
- Window title, simulator copy, and default config templates display RuleZ naming.
- RuleZ UI/command/config descriptions no longer show CCH in user-facing copy.
- New audit logs go to ~/.claude/logs/rulez.log by default.
</success_criteria>

<output>
After completion, create `.planning/phases/11-rename-fix-settings-foundation/11-01-SUMMARY.md`
</output>
