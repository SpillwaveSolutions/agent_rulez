---
phase: 20-gemini-cli-support-and-gemini-hooks-support
plan: 02
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - cch_cli/src/cli/gemini_doctor.rs
  - cch_cli/src/cli.rs
  - cch_cli/src/main.rs
  - cch_cli/tests/gemini_doctor.rs
  - docs/GEMINI_CLI_HOOKS.md
autonomous: true

must_haves:
  truths:
    - "Users can validate Gemini hook installation across project, user, system, and extension scopes."
    - "Diagnostics clearly report missing or misconfigured Gemini hook entries."
    - "Documentation explains how to run Gemini diagnostics and interpret results."
  artifacts:
    - path: "cch_cli/src/cli/gemini_doctor.rs"
      provides: "Gemini settings diagnostics and validation"
    - path: "cch_cli/tests/gemini_doctor.rs"
      provides: "Diagnostics path and output tests"
    - path: "docs/GEMINI_CLI_HOOKS.md"
      provides: "Gemini troubleshooting and diagnostics guidance"
  key_links:
    - from: "cch_cli/src/cli/gemini_doctor.rs"
      to: "~/.gemini/settings.json"
      via: "settings path resolution"
      pattern: "settings.json"
    - from: "cch_cli/src/cli/gemini_doctor.rs"
      to: "~/.gemini/extensions/<ext>/hooks/hooks.json"
      via: "extension hook discovery"
      pattern: "extensions"
---

<objective>
Add a Gemini CLI diagnostics command so users can confirm hooks are installed correctly and troubleshoot configuration issues quickly.

Purpose: Reduce setup friction by providing a deterministic validation path for Gemini hook installation.
Output: Gemini diagnostics CLI command, tests, and docs updates.
</objective>

<execution_context>
@/Users/richardhightower/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-gemini-cli-support-and-gemini-hooks-support/20-RESEARCH.md
@cch_cli/src/cli/install.rs
@cch_cli/src/cli.rs
@cch_cli/src/main.rs
@docs/GEMINI_CLI_HOOKS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Gemini diagnostics command</name>
  <files>cch_cli/src/cli/gemini_doctor.rs, cch_cli/src/cli.rs, cch_cli/src/main.rs</files>
  <action>
Add a `cch gemini doctor` command that inspects Gemini settings.json for project/user/system scopes and checks extension hook locations (both ~/.gemini/extensions/<ext>/hooks/hooks.json and ~/.gemini/hooks/). Report missing or mismatched hook entries, and provide a --json flag for machine-readable output. Ensure diagnostics are read-only and do not modify settings.
  </action>
  <verify>cargo test -p cch_cli gemini_doctor</verify>
  <done>Running the doctor command reports hook status for each scope and returns non-error output even when hooks are missing.</done>
</task>

<task type="auto">
  <name>Task 2: Add diagnostics tests and update docs</name>
  <files>cch_cli/tests/gemini_doctor.rs, docs/GEMINI_CLI_HOOKS.md</files>
  <action>
Add tests that validate diagnostics output for present/missing hooks and extension paths. Update Gemini CLI documentation with a troubleshooting section that shows how to run `cch gemini doctor`, interpret missing hook reports, and reinstall hooks when needed.
  </action>
  <verify>cargo test -p cch_cli gemini_doctor</verify>
  <done>Tests cover diagnostics output scenarios and docs include a dedicated troubleshooting section.</done>
</task>

</tasks>

<verification>
- `cargo test -p cch_cli gemini_doctor` passes
</verification>

<success_criteria>
- Users can run a single command to confirm Gemini hook installation across all scopes.
- Documentation includes diagnostics guidance and remediation steps.
</success_criteria>

<output>
After completion, create `.planning/phases/20-gemini-cli-support-and-gemini-hooks-support/20-02-SUMMARY.md`
</output>
