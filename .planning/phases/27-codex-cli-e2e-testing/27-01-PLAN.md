---
phase: 27-codex-cli-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/lib/codex_adapter.sh
  - e2e/scenarios/codex/01-install.sh
  - e2e/scenarios/codex/02-hook-fire.sh
  - e2e/scenarios/codex/03-deny.sh
  - e2e/scenarios/codex/04-inject.sh
  - e2e/fixtures/codex/hooks-hookfire.yaml
  - e2e/fixtures/codex/hooks-deny.yaml
  - e2e/fixtures/codex/hooks-inject.yaml.template
  - e2e/run.sh
autonomous: true

must_haves:
  truths:
    - "Codex CLI scenarios are discovered and executed by run.sh"
    - "If codex binary is missing, all scenarios skip (exit 77) -- no CI failure"
    - "If codex binary is present, install scenario passes (adapter detection + workspace setup)"
    - "Hook-fire, deny, and inject scenarios skip with clear 'no hook support' message"
    - "Reports include codex row in CLI x scenario matrix with skip markers for 02-04"
  artifacts:
    - path: "e2e/lib/codex_adapter.sh"
      provides: "Codex CLI adapter with check, require, setup_hooks stub, invoke_headless"
      exports: ["codex_adapter_check", "require_codex_cli", "setup_codex_hooks", "invoke_codex_headless"]
    - path: "e2e/scenarios/codex/01-install.sh"
      provides: "Install scenario -- structural check of adapter detection and workspace setup"
      contains: "scenario_install"
    - path: "e2e/scenarios/codex/02-hook-fire.sh"
      provides: "Hook-fire scenario -- unconditional skip (exit 77)"
      contains: "scenario_hook_fire"
    - path: "e2e/scenarios/codex/03-deny.sh"
      provides: "Deny scenario -- unconditional skip (exit 77)"
      contains: "scenario_deny"
    - path: "e2e/scenarios/codex/04-inject.sh"
      provides: "Inject scenario -- unconditional skip (exit 77)"
      contains: "scenario_inject"
    - path: "e2e/fixtures/codex/hooks-hookfire.yaml"
      provides: "Hookfire fixture for future use"
      contains: "e2e-hookfire-log"
    - path: "e2e/fixtures/codex/hooks-deny.yaml"
      provides: "Deny fixture for future use"
      contains: "e2e-deny-force-push"
    - path: "e2e/fixtures/codex/hooks-inject.yaml.template"
      provides: "Inject fixture template for future use"
      contains: "e2e-inject-marker"
  key_links:
    - from: "e2e/run.sh"
      to: "e2e/lib/codex_adapter.sh"
      via: "source statement at top of run.sh"
      pattern: "source.*codex_adapter.sh"
    - from: "e2e/run.sh"
      to: "CODEX_CLI_AVAILABLE"
      via: "codex availability check block in CLI loop"
      pattern: "CODEX_CLI_AVAILABLE"
    - from: "e2e/scenarios/codex/01-install.sh"
      to: "e2e/lib/codex_adapter.sh"
      via: "source + codex_adapter_check + setup_codex_hooks"
      pattern: "codex_adapter_check"
---

<objective>
Add Codex CLI adapter, 4 E2E scenarios, fixture files, and run.sh integration to the multi-CLI E2E harness.

Purpose: Complete Codex CLI E2E coverage following the established pattern from Phases 23-26. Codex has no hooks system, so scenarios 02-04 skip unconditionally while 01 validates adapter detection and workspace setup.
Output: Codex adapter, 4 scenario scripts, 3 fixture files, updated run.sh
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@e2e/lib/opencode_adapter.sh (template for codex adapter)
@e2e/run.sh (add codex source + availability block)
@e2e/scenarios/opencode/01-install.sh (template for install scenario pattern)
@e2e/scenarios/opencode/02-hook-fire.sh (template for hook-fire scenario pattern)
@e2e/fixtures/opencode/hooks-hookfire.yaml (template for fixture files)
@e2e/fixtures/opencode/hooks-deny.yaml (template for fixture files)
@e2e/fixtures/opencode/hooks-inject.yaml.template (template for fixture files)
@.planning/phases/27-codex-cli-e2e-testing/27-CONTEXT.md
@.planning/phases/27-codex-cli-e2e-testing/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Codex adapter, fixtures, and 4 scenario scripts</name>
  <files>
    e2e/lib/codex_adapter.sh
    e2e/fixtures/codex/hooks-hookfire.yaml
    e2e/fixtures/codex/hooks-deny.yaml
    e2e/fixtures/codex/hooks-inject.yaml.template
    e2e/scenarios/codex/01-install.sh
    e2e/scenarios/codex/02-hook-fire.sh
    e2e/scenarios/codex/03-deny.sh
    e2e/scenarios/codex/04-inject.sh
  </files>
  <action>
    Create `e2e/lib/codex_adapter.sh` following `opencode_adapter.sh` as the template. Export functions:
    - `codex_adapter_check()`: Check `command -v codex`, print version via `codex --version 2>&1 || true`. Error message: "Install Codex: npm install -g @openai/codex" with link to https://github.com/openai/codex.
    - `require_codex_cli()`: Return 0 if `CODEX_CLI_AVAILABLE=1`, else return 77 with skip message.
    - `setup_codex_hooks()`: Stub -- creates `${workspace}/.codex/config.toml` with `model = "o4-mini"` and `approval_policy = "never"`. Prints warning that Codex does not support hooks. Does NOT invoke any `rulez codex install` command (no such subcommand exists per user decision).
    - `invoke_codex_headless()`: Uses `codex exec "${prompt}" --ask-for-approval never --json` with `NO_COLOR=true timeout`. Timeout exit 124 maps to skip exit 77. Same pattern as `invoke_opencode_headless()` but with `codex exec` instead of `opencode run`.

    Export `CODEX_CLI_NAME="codex"`.

    Create `e2e/fixtures/codex/` directory with 3 fixture files. These are identical to opencode fixtures (canonical tool names work across CLIs via RuleZ canonicalization):
    - `hooks-hookfire.yaml`: version 1.0, rule "e2e-hookfire-log" matching Bash tools, block: false
    - `hooks-deny.yaml`: version 1.0, rule "e2e-deny-force-push" matching Bash + git push force, block: true
    - `hooks-inject.yaml.template`: version 1.0, rule "e2e-inject-marker" matching Bash, inject_command with __WORKSPACE__ placeholder

    Create `e2e/scenarios/codex/` directory with 4 scenario scripts:

    `01-install.sh`: Sources codex_adapter.sh. Defines `scenario_install(workspace, rulez_binary)`. Calls `codex_adapter_check` -- if fails, return 77 (skip). Calls `setup_codex_hooks` to write workspace config. Asserts `.codex/config.toml` exists and contains `approval_policy`. Returns 0 on success, 1 on failure. Does NOT call `rulez codex install` (no such command per user decision).

    `02-hook-fire.sh`: Sources codex_adapter.sh. Defines `scenario_hook_fire(workspace, rulez_binary)`. Prints "[skip] Codex CLI does not support hooks (no PreToolUse/BeforeTool equivalent)" and "[skip] Enable this scenario when Codex CLI adds hook support." Returns 77 unconditionally.

    `03-deny.sh`: Same skip pattern as 02. Function name: `scenario_deny`. Same skip messages.

    `04-inject.sh`: Same skip pattern as 02. Function name: `scenario_inject`. Same skip messages.

    All scripts must have `#!/usr/bin/env bash` shebang and descriptive header comments.
  </action>
  <verify>
    Run `bash -n e2e/lib/codex_adapter.sh` (syntax check).
    Run `bash -n e2e/scenarios/codex/01-install.sh` for each scenario (syntax check).
    Run `ls e2e/fixtures/codex/` to confirm 3 fixture files exist.
    Run `ls e2e/scenarios/codex/` to confirm 4 scenario scripts exist.
    Run `grep -c "scenario_" e2e/scenarios/codex/*.sh` to confirm each defines its scenario function.
  </verify>
  <done>
    codex_adapter.sh exports 4 functions (check, require, setup_hooks, invoke_headless). 4 scenario scripts define their respective scenario functions. 3 fixture files match opencode fixture format. All files pass bash syntax check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate codex adapter into run.sh</name>
  <files>e2e/run.sh</files>
  <action>
    Modify `e2e/run.sh` to add Codex CLI support:

    1. Add source line after the opencode_adapter.sh source (around line 37):
       ```
       # shellcheck source=lib/codex_adapter.sh
       source "${E2E_ROOT}/lib/codex_adapter.sh"
       ```

    2. Add codex availability check block after the opencode block (around line 153), following the exact same pattern:
       ```
       if [[ "${cli_name}" == "codex" ]]; then
         if codex_adapter_check > /dev/null 2>&1; then
           CODEX_CLI_AVAILABLE=1
         else
           CODEX_CLI_AVAILABLE=0
           echo "  NOTE: codex CLI not available — scenarios requiring it will be skipped" >&2
         fi
         export CODEX_CLI_AVAILABLE
       fi
       ```

    No other changes to run.sh. The dynamic scenario discovery will automatically find `e2e/scenarios/codex/*.sh`.
  </action>
  <verify>
    Run `bash -n e2e/run.sh` (syntax check passes).
    Run `grep "codex_adapter" e2e/run.sh` to confirm source line and availability check are present.
    Run `grep "CODEX_CLI_AVAILABLE" e2e/run.sh` to confirm the environment variable is set and exported.
  </verify>
  <done>
    run.sh sources codex_adapter.sh, sets CODEX_CLI_AVAILABLE based on adapter check, and auto-discovers codex scenarios from e2e/scenarios/codex/.
  </done>
</task>

</tasks>

<verification>
1. `bash -n e2e/run.sh` passes (no syntax errors after modification)
2. `bash -n e2e/lib/codex_adapter.sh` passes
3. `bash -n e2e/scenarios/codex/01-install.sh` passes for all 4 scenarios
4. `grep "codex" e2e/run.sh` shows source line + availability block
5. `ls e2e/scenarios/codex/` shows 4 scenario scripts
6. `ls e2e/fixtures/codex/` shows 3 fixture files
7. Run `./e2e/run.sh --cli codex` -- if codex is not installed, all 4 scenarios skip (exit 77); if codex IS installed, 01 passes and 02-04 skip
</verification>

<success_criteria>
- Codex adapter file exists with all 4 exported functions
- All 4 scenario scripts pass bash syntax check and define correct scenario functions
- Fixture files match the established format (identical to opencode fixtures)
- run.sh sources codex adapter and has availability check block
- Running `./e2e/run.sh --cli codex` produces correct skip/pass results with no failures
- Reports show codex row with skip markers for hook-dependent scenarios (02-04)
</success_criteria>

<output>
After completion, create `.planning/phases/27-codex-cli-e2e-testing/27-01-SUMMARY.md`
</output>
