---
phase: 26-opencode-cli-e2e-testing
task: 0
total_tasks: 0
status: research_incomplete
last_updated: 2026-03-05T22:55:48.580Z
---

<current_state>
Phase 26 planning started but research not yet completed. No RESEARCH.md, no PLAN.md files created yet. The phase directory exists with shared E2E-CONTEXT.md and README.md only.

We were in the `/gsd:plan-phase 26` workflow, at the research step.
</current_state>

<completed_work>

- Phase 25 (Copilot CLI E2E Testing): Fully complete with 3/3 plans executed and verified (5/5 must-haves)
- Phase 26 init: gsd-tools init validated phase exists, confirmed no research/plans yet
- Phase 26 roadmap section read: Goal is "Add OpenCode CLI adapter + scenarios to the existing E2E harness"
</completed_work>

<remaining_work>

- Research: Create 26-RESEARCH.md — investigate OpenCode CLI specifics:
  1. OpenCode uses a PLUGIN system (not traditional hooks) — user confirmed this. Lifecycle event callbacks serve as hook-like mechanism. Research how RuleZ integrates with this.
  2. Read `rulez/src/adapters/opencode.rs` and `rulez/src/cli/opencode.rs` for RuleZ's OpenCode integration (hook config path, format, install behavior)
  3. OpenCode headless invocation flags (equivalent of Claude's `-p`, Gemini's `--yolo`, Copilot's `--allow-all-tools`)
  4. OpenCode auth mechanism (API key? OAuth? provider config?)
  5. Check local availability: `which opencode`, `opencode --help`
- Planning: Create 26-01-PLAN.md (adapter + fixtures + run.sh) and 26-02-PLAN.md (4 scenario scripts) — follows Phase 24/25 pattern
- Plan verification: Run gsd-plan-checker
</remaining_work>

<decisions_made>

- Phase 26 follows the same 2-plan structure as Phases 24 (Gemini) and 25 (Copilot): Plan 01 = adapter + fixtures + run.sh, Plan 02 = 4 scenarios
- User clarified: OpenCode uses a plugin system with lifecycle event callbacks, NOT traditional hooks. Close enough to hooks for E2E testing purposes.
</decisions_made>

<blockers>

- Research agent kept getting rejected by user (unclear why — possibly too heavyweight or wrong approach)
- User suggested doing research directly via local file reads instead of spawning researcher agent
</blockers>

<context>
The established pattern from Phases 23-25 is very clear:
- Adapter library (e2e/lib/<cli>_adapter.sh) with 4 functions: check, require, setup_hooks, invoke_headless
- 3 fixture YAML files (hookfire, deny, inject template) — content identical across CLIs
- run.sh updated with source + availability block
- 4 scenario scripts (01-install, 02-hook-fire, 03-deny, 04-inject)

The key unknowns are OpenCode-specific: plugin config format, headless flags, auth mechanism. These should all be answerable from the RuleZ adapter source code + `opencode --help`.
</context>

<next_action>
Start with: Read `rulez/src/adapters/opencode.rs` and `rulez/src/cli/opencode.rs` (or find equivalent files via glob) to understand how RuleZ integrates with OpenCode's plugin system. Then check `opencode --help` for headless flags. This gives enough info to write RESEARCH.md and proceed to planning.
</next_action>
