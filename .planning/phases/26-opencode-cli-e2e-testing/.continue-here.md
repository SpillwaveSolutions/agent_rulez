---
phase: 26-opencode-cli-e2e-testing
task: 6
total_tasks: 7
status: partially_complete
last_updated: 2026-03-06T01:00:00Z
---

<current_state>
Phase 26 implementation is partially complete. Core deliverables done, headless E2E integration blocked on OpenCode plugin discovery.

**What's done:**
1. Fixed stale `cch` references in `opencode_install.rs` (resolve_binary_path, is_rulez_hook, print message)
2. Created OpenCode E2E fixtures (3 YAML files)
3. Created OpenCode adapter (`e2e/lib/opencode_adapter.sh`)
4. Created 4 scenario scripts (`e2e/scenarios/opencode/`)
5. Updated `e2e/run.sh` with opencode adapter source + availability check
6. Created TypeScript plugin (`opencode-plugin/rulez-plugin/`)
7. Created research doc (`.planning/phases/26-opencode-cli-e2e-testing/26-RESEARCH.md`)
8. CI passes: fmt, clippy, test all green

**What's blocked:**
Headless E2E scenarios 02-04 (hook-fire, deny, inject) fail because OpenCode doesn't load the plugin from the E2E workspace's `.opencode/plugin/` directory. OpenCode's plugin auto-discovery mechanism needs further investigation.
</current_state>

<completed_work>
- Deliverable 1 (Research doc): Complete
- Deliverable 2 (Adapter): Complete (both settings.json and plugin approaches written)
- Deliverable 3 (Scenarios): Complete (4 scripts)
- Deliverable 4 (Fixtures): Complete (3 files)
- Deliverable 5 (run.sh): Complete
- Deliverable 6 (Fix stale cch refs): Complete
- Deliverable 7 (TypeScript plugin): Complete
- Install scenario: PASSES
- Headless scenarios: FAIL (plugin not loaded)
</completed_work>

<remaining_work>
1. **Debug OpenCode plugin discovery**: Figure out why `.opencode/plugin/rulez-e2e.ts` isn't loaded
   - Possible causes: OpenCode may not support project-level `.opencode/plugin/` discovery
   - May require global plugin installation at `~/.config/opencode/plugins/`
   - May need different export format (named export vs default export)
   - May need `opencode.json` `"plugin"` key (array format, not `"plugins"` dict)

2. **Alternative approach**: Instead of plugin-based hooks, investigate if OpenCode supports command-based hooks natively (the `hooks` key in `.opencode/settings.json` that `rulez opencode install` generates)

3. **Fallback**: If neither plugin nor command hooks work from workspace, change headless scenarios to properly SKIP rather than FAIL when hooks aren't firing
</remaining_work>

<decisions_made>
- OpenCode headless invocation: `opencode run "message" --format json` (not `--prompt`)
- Plugin format: TypeScript in `.opencode/plugin/` using Bun.spawn to call rulez binary
- Also wrote `.opencode/settings.json` (command-based hooks) as fallback
- Fixed all stale `cch` references including variable name `cch_path` -> `rulez_path`
- `is_rulez_hook()` detects both `"rulez"` and `"cch"` for backwards compat
</decisions_made>

<blockers>
OpenCode plugin auto-discovery doesn't work from project-level `.opencode/plugin/` in E2E workspace directories. This blocks scenarios 02-04.
</blockers>

<next_action>
Research OpenCode's plugin loading mechanism more deeply:
1. Check if plugins need to be in `~/.config/opencode/plugins/` (global) vs `.opencode/plugin/` (project)
2. Check if the export format needs to be different
3. Try: `opencode debug config` from within an E2E workspace to see if the plugin is listed
4. Try installing the plugin globally for E2E testing
</next_action>
